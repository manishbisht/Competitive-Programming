#include <bits/stdc++.h>
using namespace std;
long long int n, temp, i, j;
string s;
int main()
{
	cin>>s;
	temp=1;
	if(s.length()%2==0){
        temp=s.length()/2;
	}
	else {
        temp=s.length()/2;
	}
	j=s.length()-1;
	for(i=0;i<temp;i++){
        if(a[i]==a[j])
	}
	if(temp==0){
        cout<<"NIE"<<endl;
	}
	else {
        cout<<"TAK"<<endl;
	}
    return 0;
}
