#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<long long int> v;
    long long int n, i;
    cin>>n;
    v.resize(n);
    for(i = 0; i < n; ++i){
        cin>>v[i];
    }
    sort(v.begin(), v.end());
    for(i = 0; i < n - 3; ++i){

    }
    return 0;
}
