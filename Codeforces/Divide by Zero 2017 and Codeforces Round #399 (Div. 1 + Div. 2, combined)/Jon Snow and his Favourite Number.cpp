#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long int n, a, b, i;
    vector<long long int> v;
    cin>>n>>a>>b;
    v.resize(n);
    for(i = 0; i < n; ++i){
        cin>>v[i];
    }
    cout<<ans<<endl;
    return 0;
}
