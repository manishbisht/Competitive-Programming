#include <bits/stdc++.h>
using namespace std;
long long int i, ans;
string s;
int main()
{
    cin>>s;
    sort(s.begin(), s.end());
    reverse(s.begin(), s.end());
    for(i=1;i<n;i++){

    }
    return 0;
}
